import { Lang_settings } from "../../types/lang/settings";

const settings: Lang_settings = {
    settings: "Settings",
    exit_without_save: "Exit without save",
    save: "Save",
    name: "Name",
    description: "Description"
}

export default settings